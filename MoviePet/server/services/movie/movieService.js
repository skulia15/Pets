'use strict';

class MovieService {
    constructor() {
        //this.connection = require(database);
    }

    
    //example servicefn
    /**
     * Returns a list of movies with their associated users
     * @returns {Array} An array of movies and their associated users
     */
    
    // findAll(loanDate, loanDuration) {
    //     return new Promise((resolve, reject) => {
    //         this.connection.Movie.findAll().then((books) => {
    //             for (let i = 0; i < movies.length; i++) {
    //                 movies[i] = movies[i].toJSON();
    //                 movies[i].publishedDate = moment(movies[i].publishedDate).format('ll');
    //             }
    //             resolve(movies);
    //         }).catch((reason) => {
    //             reject(reason);
    //         });
    //     });
    // }
}
module.exports = new MovieService();